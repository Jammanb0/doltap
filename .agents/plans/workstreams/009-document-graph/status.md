# 문서 그래프 기반 — 상태

> 배경과 완료 조건은 `README.md`, 단계와 검증 방법은 `plan.md`, 정한 것과 그
> 이유는 `decisions.md`에 있습니다. 여기는 지금 어디까지 왔고 다음이 무엇인지만
> 다룹니다.

## 작업 위치

| 항목 | 값 |
| --- | --- |
| 작업 위치 | `workstream/009-document-graph` |
| 기준 브랜치 | `main` |
| 반영 대상 브랜치 | `main` |
| 원격 작업 흔적 | 남기지 않음 — 원격 작업 브랜치·PR·병합 커밋 없음 |

이 값들은 하나씩 묻지 않고 006·007·008의 관례를 따랐습니다. 사용자가 "로컬
브랜치 하나 만들어서 진행하고, 대작업 세팅만 해줘"라고 했습니다. 계획 커밋도
기준 브랜치가 아니라 이 브랜치에 둡니다. 그 이유와 대가는 `decisions.md`에
있습니다.

**받은 승인의 범위**

- 워크스트림 생성 — 받음 ("다음 대작업 세팅을 해야해. 진행해줘")
- 이 브랜치 안의 파일 변경과 로컬 커밋 — 받음 ("로컬 커밋을 승인하며")
- push — **받지 않음** ("푸시는 진행하지 말아줘")
- `main` 반영 — **받지 않음**
- 구현 착수 — **받지 않음.** 사용자가 세팅만 요청했고, 다른 에이전트의 검토를
  받은 뒤에 코드를 고치겠다고 했습니다
- 대작업 종료와 아카이브 — **받지 않음**
- 작업 브랜치 삭제 — **받지 않음**

## 현재 단계

세팅만 마쳤습니다. `plan.md`의 1단계에 들어가기 전입니다.

## 직전 완료

- 설계 기록 `docs/document-graph-design.md`를 커밋했습니다. 지금까지 추적되지
  않은 파일이었습니다.
- 이 폴더의 네 문서를 쓰고 `current.md`에 등록했습니다.
- `npm test`, 검사기의 `check template`과 `check .`를 돌려 통과를 확인했습니다.
  결과는 아래 「검증」에 있습니다.

## 다음 행동

1. 다른 에이전트에게 이 세팅을 검토받습니다. 사용자가 그렇게 하겠다고 했습니다.
2. 사용자와 새 공개 이름을 정합니다 — `plan.md` 1단계. 다섯 값을 한 번에
   정합니다.

이름을 기다리는 동안 `plan.md` 2단계에서 이름에 의존하지 않는 항목(정규화 규칙,
관계 어휘, 렌더러 동작 확인)을 먼저 볼 수 있습니다. 다만 **구현 착수 승인을
아직 받지 않았습니다.**

## 검증

세팅 시점에 돌린 것입니다. 코드는 고치지 않았으므로 기준선 확인입니다.

| 확인 | 결과 |
| --- | --- |
| `npm test` | 42개 통과 |
| `node bin/cairn.mjs check template` | 문제 0, 확인 5 |
| `node bin/cairn.mjs check .` | 문제 0, 확인 0, 진행 중인 대작업 1개 |

`check template`의 확인 5개는 골격이 일부러 비워 둔 채우기 자리입니다. 손대지
않았고 이 대작업 전에도 같은 수였습니다.

## 막힌 점과 열린 질문

- **새 공개 이름이 정해지지 않았습니다. 여기서 대작업 전체가 막힙니다.**
  운영 폴더와 앵커 접두사가 이름에서 나오므로, 정해지기 전에는 불변 ID를
  발급하지 않고 폴더·명령 이름도 바꾸지 않습니다.
- **이 구조를 쓰는 외부 프로젝트가 있는지 확인되지 않았습니다.** 없다고
  단정하지 않습니다. 확인되지 않았다는 것과 없다는 것을 섞어 쓴 것이 007에서
  고친 실수입니다.
- 설계 기록의 미확정 항목 27개가 그대로 남아 있습니다. `plan.md` 2단계에서
  각각 확정·시험 대상·범위 밖으로 가릅니다.
- 가장 어려운 항목은 해시 계산이 아니라 정규화 규칙입니다. 잘못 정하면 서식만
  바꿔도 재검토가 쏟아지거나 중요한 변경을 놓칩니다. 소스만 읽어서는 오탐을
  다 찾지 못하고 실제 문서를 고치는 동안 드러납니다.
- `plan.md`의 3단계 아래는 형식이 확정되면 모양이 달라질 수 있습니다. 지금
  순서는 확정되지 않은 형식 위에 세운 것입니다.
