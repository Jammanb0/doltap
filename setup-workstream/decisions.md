# doltap 구조 적용 — 결정

<!-- 이 문서는 골격 위치의 `setup-workstream/`에서 복사해 온 것입니다.
     `.doltap/plans/workstreams/<번호>-doltap-setup/decisions.md`에 놓습니다.
     정한 것이 하나도 없으면 이 파일을 두지 않아도 됩니다. -->

> 적용하면서 정한 것과 그 이유를 남깁니다. 특히 `plan.md` 5번에서 기존 규칙과
> 골격이 어긋났을 때 어느 쪽을 따르기로 했는지를 여기에 적습니다.

<!-- 채우기: 결정마다 `##` 제목으로 무엇을 정했는지 쓰고, 바로 아래에
     `계속 유효` 또는 `이번만`을 한 줄 붙입니다. 계속 유효한 것에는 담당 활성
     원본을, 아직 옮기지 않았으면 옮길 곳을 같은 줄에 적습니다.
     예) 커밋 메시지는 기존 git log를 따라 영어로 유지한다 → `계속 유효`,
         활성 원본은 AGENTS.md 의 커밋 규칙
     예) `.doltap/`는 커밋한다. 다른 기기에서 이어받을 수 있어야 한다 → `이번만` -->
