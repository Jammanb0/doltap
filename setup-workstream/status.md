# doltap 구조 적용 — 상태

<!-- 이 문서는 골격 위치의 `setup-workstream/`에서 복사해 온 것입니다.
     `.doltap/plans/workstreams/<번호>-doltap-setup/status.md`에 놓습니다. -->

> 배경과 완료 조건은 `README.md`, 적용 단계는 `plan.md`에 있습니다. 여기는 지금
> 어디까지 왔고 다음이 무엇인지만 다룹니다.

## 골격 위치

<!-- 채우기: 실제 값을 적습니다. `plan.md`의 명령이 이 값을 씁니다.
     형태는 `내부 임시 클론`, `외부 임시 클론`, `외부 기존 폴더`,
     `저장소 루트` 중 하나입니다. 정리 대상은 이번 적용을 위해 마련한 임시
     골격일 때만 `예`입니다.

     **절대 경로를 여기에 쓰지 않습니다.** 이 파일은 적용 초반의 부트스트랩
     커밋에 들어갑니다. 나중에 문장에서 지워도 그 커밋에는 운영체제 사용자
     이름이 그대로 남고, 이력은 되돌리기 어렵습니다.

     **개인정보가 들어가지 않는 상대 경로만 적습니다** — `.doltap-bootstrap/`,
     `../doltap/` 처럼요. 상대 경로라고 안전한 것이 아닙니다. 프로젝트가
     `C:/work/app`이고 골격이 `C:/Users/이름/doltap`이면 상대 경로는
     `../Users/이름/doltap`이 되어 사용자 이름이 그대로 들어갑니다.

     아래 중 하나라도 걸리면 **경로를 적지 말고 아래 「경로를 적을 수 없을 때」를
     따릅니다.**

     - 상대 경로로 가리킬 수 없다 (드라이브가 다르거나 전혀 다른 트리)
     - 상대 경로에 사람 이름이나 계정 이름이 보인다

     **적는 경로는 셸 따옴표 없이 있는 그대로 적습니다.** 공백이나 특수문자가
     있어도 그대로 두고, 명령을 만들 때 지금 쓰는 셸에 맞게 인용합니다.
     `plan.md`의 `<골격 위치>`와 `<임시 골격 경로>`는 자리표시자이지 그대로
     실행하는 문자열이 아닙니다. 작은따옴표가 든 경로는 `'...'`로 감싸는 것만
     으로는 깨집니다.

- 상대 경로: .doltap-bootstrap/
- 형태: 내부 임시 클론
- 정리 대상: 예

     예) 상대 경로: ../my doltap / 형태: 외부 임시 클론 / 정리 대상: 예
-->

<!-- 고르기: 경로를 적을 수 없을 때만 위 대신 아래를 남깁니다.

- 상대 경로: 적지 않습니다. 상대 경로로 가리킬 수 없는 자리입니다
- 형태: 외부 임시 클론
- 정리 대상: 예

이어받는 세션은 골격 경로를 사용자에게 한 번 묻고 그 세션 안에서만 씁니다.
문서에 적지 않습니다. 묻지 못하면 골격이 필요한 단계를 진행하지 않고 알립니다.
-->

## 작업 위치

<!-- 채우기: 골격 위치의 `APPLY.md` 2번에서 정한 것을 적습니다.

- 작업 위치: `workstream/<번호>-doltap-setup`
  (직접 작업이면 "`main`에서 직접"이라고 적습니다)
- 기준 브랜치: `main`
- 반영 대상 브랜치: `main`
- 원격 작업 흔적: 남기지 않음
  (직접 작업이면 `해당 없음`, 원격 브랜치와 PR을 쓰면 `남김`이라고 적습니다)
-->

## 현재 단계

<!-- 채우기: 지금 몇 단계까지 왔는지 한두 줄. 예) 2단계 진행 중. -->

`plan.md`의 1단계 시작 전입니다.

## 직전 완료

<!-- 채우기: 방금 끝낸 것을 한두 줄. 지나간 단계를 쌓아 두지 않습니다. -->

## 다음 행동

<!-- 채우기: 바로 다음에 할 것 한 줄. -->

## 막힌 점과 열린 질문

<!-- 채우기: 사용자에게 물어야 하는데 아직 답을 못 받은 것. -->
