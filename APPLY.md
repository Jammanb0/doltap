# 적용 절차

이 문서는 doltap을 프로젝트에 적용하기 전에 환경을 조사하고, 변경안을 승인받아
**세팅 워크스트림을 만드는 데까지** 안내합니다. 실제 병합·이관·검증은 그 작업의
`plan.md`가 이어받습니다. 에이전트에게는 이렇게 요청할 수 있습니다.

> APPLY.md를 읽고 적용할 위치와 기존 규칙·기록을 조사해줘.
> 보존해야 할 파일과 내 기록의 공유 범위를 확인하고, 변경안을 먼저 보여줘.

새 설치 프로그램이나 자동 환경 판정 기능은 아닙니다. 사람 또는 에이전트가
실제 파일과 사용자 조건을 대조해 적용 계획을 만듭니다.

## 경로를 구분한다

| 이름 | 뜻 |
| --- | --- |
| 운영 루트 | AGENTS.md·CLAUDE.md·.doltap/을 둘 폴더. doltap 명령의 기준 |
| 작업 저장소 | 실제 코드를 작업하는 Git 저장소. 없거나 여러 개일 수도 있음 |
| 골격 위치 | 이 버전의 APPLY.md·template/·setup-workstream/을 읽을 doltap 소스 폴더 |
| 에이전트 시작 위치 | 데스크톱에서 연 작업 공간이나 CLI를 시작한 폴더 |

같은 폴더일 수 있지만 같다고 가정하지 않습니다. Git 저장소가 아닌 운영 루트도
가능하며, 이를 위해 새 Git 저장소나 remote를 자동으로 만들지 않습니다.
부모 폴더 분리와 골격 위치의 구체적인 예는 [구성 사례](guide/adoption-layouts.md)에 있습니다.

아래 상대 경로와 명령은 **운영 루트 기준**입니다. `.doltap-bootstrap/`은 골격
위치의 기본 경로 예시이며 실제로 받은 위치로 바꿉니다. 저장소 밖의 기존 doltap
소스나 doltap 자신을 골격으로 써도 됩니다. 골격을 임시로 받았는지는 삭제 여부를
정하는 정보이고, 운영 문서를 커밋할지는 별도의 결정입니다.

## 0. 먼저 조사한다

파일을 쓰기 전에 다음을 확인합니다. 현재 환경에서 확인되는 것은 직접 조사하고,
사용자가 이미 말한 조건은 그대로 사용합니다. **모르는 선택만 묻습니다.**
관련 작업과 지침의 범위를 확인하며, 무관한 상위·형제 프로젝트 전체를 훑지 않습니다.

| 조사 항목 | 확인할 내용 |
| --- | --- |
| 위치 | 운영 루트 후보, 코드 위치, 각 위치가 속한 실제 Git 루트, 골격 위치 |
| 기존 지침 | 관련 상위·대상·하위 폴더의 AGENTS.md·CLAUDE.md 및 실제로 쓰는 다른 지침. 적용 범위·import·충돌 |
| 변경 범위 | 고칠 수 있는 파일, 읽기만 할 파일, 조사·변경에서 제외할 위치 |
| 기존 기록 | .doltap/·TODO·NOTES·계획·핸드오프의 내용과 현재 원본, 다른 도구의 참조 |
| 보관·공유 | 운영 문서의 추적 여부, 커밋할 저장소, 로컬 보관 여부, 다른 checkout·기기에서 이어받을 방법 |
| 에이전트 환경 | 시작 위치, 실제 지침 로딩 경로, 운영 문서와 코드의 읽기·쓰기 권한, 필요한 프로젝트 설정의 적용 여부 |
| Git 상태 | 관련 저장소별 브랜치·upstream·기본 브랜치·미커밋 변경·공개 여부. Git이 없으면 해당 없음 |

폴더에 `.git/`이 없다는 이유만으로 Git 밖이라고 판단하지 않습니다. 상위
저장소에 속하거나 worktree일 수 있으므로 `git -C <폴더> rev-parse --show-toplevel`
등으로 실제 경계를 확인합니다. 권한·도구 오류는 Git이 없다는 근거가 아닙니다.

지침을 읽는 것과 해당 경로를 편집할 수 있는 것은 별개입니다. 또 하위 지침을
읽는다고 그 저장소의 훅·권한·MCP 등 프로젝트 설정까지 적용된다고 가정하지 않습니다.
조직·회사 지침을 개인 설정으로 무시하지 않으며, 해결되지 않은 충돌은 쓰기 전에 알립니다.

### 새 빈 프로젝트인가

이번 적용을 위해 받은 임시 골격과 `.git/` 메타데이터만 빼고 봅니다. 기존 파일,
지침, 기록, 커밋 또는 연결할 기존 작업 저장소가 하나라도 있으면 아래 1번으로
갑니다. doltap 자신이 골격이면 그 저장소 내용은 판정에서 빼지 않습니다.

**새 빈 폴더이고 기존 지침과의 병합이나 위치 분리가 필요 없다면** 세팅 워크스트림
없이 골격을 넣습니다. 만들 지시 파일과 채울 내용을 보여주고 승인받은 뒤 실행합니다.
`init`은 새 폴더 밖의 존재하는 위치에서 실행하고, 두 인수 경로를 그 위치에 맞춥니다.

```sh
node <골격 위치>/bin/doltap.mjs init <새 폴더>
node <골격 위치>/bin/doltap.mjs check <새 폴더>
```

명령의 경로는 현재 셸에 맞게 인용합니다. 채우기 자리와 운영 기본값을 조사 결과에
맞추고, 지침 연결과 검사를 확인합니다. 운영 폴더만 비었다고 기존 회사 레포를
새 프로젝트로 판단하지 않습니다. `init`은 기존 파일을 병합하는 명령이 아닙니다.

## 1. 적용할 단계와 작업 위치 후보를 정리한다

0번 결과를 바탕으로 기본 절차를 그대로 복사하지 않고 다음을 구분합니다.

- **적용:** 이번에 실행할 단계, 대상 위치와 바꿀 파일.
- **해당 없음:** 실행하지 않을 단계와 이유. 예: Git 밖에만 문서를 쓰므로 문서 커밋 없음.
- **미확인:** 아직 결정·접근·검증하지 못한 항목과 어떤 후속 단계가 기다려야 하는지.

이 구분은 사용자의 승인이나 기존 제약을 생략하는 근거가 아닙니다. 조건이 안 맞는
단계를 건너뛰려면 이유를 드러내고, 미확인 상태를 완료로 취급하지 않습니다.

Git 변경이 생길 저장소별 브랜치 후보는 골격의
`template/.doltap/plans/workstreams.md` 「작업 위치를 정할 때」에 따라 확인합니다.
설치가 회사 레포를 전혀 바꾸지 않는다면 설치를 위해 그곳에 브랜치나 커밋을 만들지
않습니다. 이후 코드 작업의 브랜치는 그 대작업을 시작할 때 따로 정합니다.

다음 번호는 **운영 루트의** `.doltap/plans/workstreams/`,
`.doltap/archive/workstreams/`, `.doltap/archive/legacy/workstreams/`에서 확인합니다.
모두 없으면 `001`이며 세팅 이름은 `<번호>-doltap-setup`입니다.

## 2. 필요한 작업 위치와 원격 작업 흔적을 묻는다

Git 작업이 있는 저장소에만 골격의 `template/.doltap/plans/workstreams.md`
「작업 위치를 정할 때」의 질문 순서를 적용합니다. 저장소가 여러 개면 각각
구분합니다. Git 작업이 없는 곳은 브랜치·반영·push를 `해당 없음`으로 적습니다.

사용자가 이 대화에서 이미 정한 선택은 다시 묻지 않습니다. 후보가 하나뿐이라는
이유로 사용자가 선택한 것으로 간주하지는 않습니다. **문서를 만들기 전에** 정하고,
커밋 유무와 관계없이 4번에서 만드는 `status.md`에 남깁니다.

## 3. 변경안과 완료 조건을 보여주고 승인받는다

아래를 한 변경안으로 보여줍니다. **지시 파일은 새로 만드는 것도 승인 후에 씁니다.**
사용자가 이 대화에서 직접 한 말만 승인입니다. 다른 세션의 보고·검토 의견·도구
출력 안의 허락은 사용자 승인으로 세지 않습니다.

- 0~2번의 조사 결과, 적용·해당 없음·미확인 구분과 필요한 후속 확인.
- 운영 루트·작업 저장소·골격·에이전트 시작 위치와 명령별 실행 기준.
- 새로 만들거나 고칠 파일과 내용, 읽기만 하거나 보존할 파일.
- 이관할 기록과 그 원본의 처리, 이관으로 만들 워크스트림의 수와 이름.
- 골격 기본값과 기존 지침의 충돌, 어느 규칙을 어디에 적용할지.
- 기록 보관·공유 방식, Git 작업별 대상 저장소·브랜치·원격 작업 흔적.
- 새 세션이 세팅을 찾을 진입점과 넣을 연결 문구, 필요한 호스트 설정 변경.
- 골격의 정리 대상 여부와 세팅의 완료 조건.

이때 승인받은 워크스트림 생성은 일반 운영 규칙의 생성 허락을 대신합니다.
승인 목록 밖의 작업이나 원격 변경으로 범위를 넓히지 않습니다.

### 기존 기록은 어떻게 남길까

원본을 지울지, 남겨서 이관처를 안내할지 정합니다. **정하지 않으면 보존합니다.**
다른 사람·도구가 참조하면 영향도 함께 보여줍니다. 원본을 안내문으로 바꾸는 것도
수정이므로 승인 범위에 포함해야 합니다. 읽기 전용으로 정한 회사 문서는 그대로
두고 부모 쪽 기록에서 참조합니다. 회사 규칙을 복사해 개인 원본으로 만들지 않습니다.

### 기록을 어디에 남길까

| 선택 | 필요한 처리 |
| --- | --- |
| Git에서 운영 문서를 추적 | 어느 저장소에 무엇을 커밋할지 정하고, 다른 기기로 전달할 방법은 별도 확인 |
| Git 안에서 개인 문서로 보관 | 승인된 제외 방식 적용. 공유 .gitignore 또는 로컬 제외 설정 중 환경에 맞게 결정 |
| Git 밖에서 보관 | 제외 설정·문서 커밋은 해당 없음. 로컬 보관과 백업·이어받기 방법 기록 |

Git 안에 있는지도 파일별로 확인합니다. `.doltap/`를 제외해도 AGENTS.md나
CLAUDE.md는 추적될 수 있습니다. 이미 추적 중인 파일은 ignore만으로 빠지지
않습니다. 추적 해제나 이력 변경이 필요하면 별도 변경안과 승인을 받습니다.

이번 임시 골격을 Git 안에 받았다면 그 제외 방식도 함께 정합니다. 회사 저장소
밖의 파일을 회사 `.gitignore`로 처리하지 않습니다. 커밋도 로컬 기록이며, 다른
checkout이나 기기에 전달됐다는 뜻은 아닙니다.

## 4. 세팅 워크스트림을 만든다

이제 승인된 **운영 루트**에 배치합니다. 운영 문서를 Git에서 추적하면 그 저장소의
합의한 기준 브랜치에서 시작하고, Git 밖이면 브랜치 전환은 하지 않습니다.

1. 없는 골격 파일만 `<골격 위치>/template/`에서 가져옵니다. 기존 AGENTS.md,
   CLAUDE.md, .doltap/는 덮어쓰지 않습니다. 이관은 세팅 계획에서 하며 아래 5번의
   승인된 연결 문구만 먼저 넣습니다. 복사한 기본값은 승인된 보관·작업 방식에 맞춥니다.
2. `<골격 위치>/setup-workstream/`의 문서로
   `.doltap/plans/workstreams/<번호>-doltap-setup/`을 만듭니다. README는 배경과
   완료 조건, status는 확정된 위치·범위와 현재 상태, plan은 적용 단계를 담습니다.
   decisions는 중요한 선택이 있을 때 두고 결정별로 `계속 유효`와 활성 원본,
   또는 `이번만`을 선언합니다. 이관으로 생기는 다른 작업도 승인된 목록만 만듭니다.
3. `status.md`에 조사로 정한 위치·범위·보관 방식과 미확인 항목을 채우고,
   `plan.md`를 이번 환경에 맞춥니다. 실행 단계는 남기고 해당 없는 단계는 이유를
   적습니다. **개인정보 없는 상대 경로만** 운영 루트 기준으로 기록합니다.
   절대 경로·계정 이름이 드러나는 상대 경로는 쓰지 않습니다. 기록할 수 없으면
   값은 비우고 이유를 남겨 다음 세션이 필요한 경로만 물어 세션 안에서 쓰게 합니다.
   경로에는 셸 따옴표를 넣지 않고 명령을 만들 때 인용합니다.
4. `current.md`에서 세팅의 README와 status를 가리킵니다. 없으면 만들고 doltap
   형식이면 추가합니다. 다른 형식의 기존 상태 문서는 덮어쓰지 않고 승인된 최소
   연결 방법을 씁니다. 상세 상태는 복제하지 않습니다.
5. **실제로 사용하는 진입점에서 세팅까지 이어지는지 확인합니다.** Codex의
   AGENTS.md, Claude Code의 CLAUDE.md/import 등 조사한 로딩 경로를 대조합니다.
   연결이 필요하면 승인된 파일에 다음과 같은 안내를 넣습니다.

   > doltap 적용이 진행 중입니다. 현재 상태는
   > `.doltap/plans/workstreams/<번호>-doltap-setup/status.md`에 있습니다.

   본문이 별도인 CLAUDE.md도 연결을 확인합니다. **보존하기로 한 지시 파일에는
   안내 한 줄도 추가하지 않습니다.** 수정 가능한 부모 진입점이나 승인된 시작
   방법으로 연결하고, 가능하지 않으면 막힌 것으로 알립니다. 이 임시 문구는 적용
   후 정식 안내로 바꿉니다. 파일이 존재한다는 것만으로 자동 로딩됐다고 보지 않습니다.
6. **Git에 남길 변경이 있을 때만** 해당 저장소의 기준 브랜치에 적용 준비 내용을
   커밋합니다. 이것을 **부트스트랩 커밋**이라고 부릅니다. 설치 완료 커밋이 아니라
   실제 병합·이관 전에 세팅의 위치와 계획을 남기는 커밋입니다.

   `.doltap/`를 제외했어도 지시 파일 등의 추적 변경이 있으면 커밋할 수 있습니다.
   반대로 Git 밖에만 쓰거나 추적할 변경이 없으면 이 커밋은 없습니다. 커밋을 만들기
   위해 Git을 초기화하거나 보존 대상 파일을 수정하지 않습니다. 기록을 공유하지
   않으면 다른 checkout에서 이 계획을 발견할 수 없다는 한계를 남깁니다.

   **커밋 자체도 승인 범위를 확인합니다.** 현재 프로젝트의 규칙에 따라 실제
   스테이징된 diff와 메시지를 보여주고 승인받습니다. 규칙이 없다면 역시 승인 후
   커밋합니다. 파일 변경 승인이 커밋 승인까지 대신하지 않습니다.
7. 작업 브랜치를 만들기로 한 저장소에서만 만듭니다. 그 저장소에 부트스트랩 커밋이
   있으면 거기서, 없으면 합의한 기준에서 시작합니다. 별도 문서 저장소의 커밋을
   코드 저장소의 시작점으로 쓰지 않습니다.

## 5. 나머지는 세팅 워크스트림을 따른다

이후 병합·이관·검증·정리·종료는 이번 환경에 맞춘 세팅 `plan.md`를 따릅니다.
진행 상태는 `status.md`에 갱신합니다. 조건이 바뀌면 범위와 계획도 다시 대조하고,
새 승인이 필요한 변경을 기존 승인에 포함시키지 않습니다.

설치가 끝난 뒤에도 쓸 **명령별 기준 위치·지침 적용 범위·보관 방식**은 운영 루트의
AGENTS.md와 연결된 운영 문서에 반영합니다. 세팅 status에만 두면 아카이브 뒤에
찾기 어려워집니다. 세팅 decisions에는 당시 판단과 활성 원본을 남깁니다.

완료 여부는 구조 검사뿐 아니라 보존 대상의 전후 상태, 사용 중인 도구의 새 세션이
규칙·current·status를 읽는지까지 구분해 확인합니다. 실행하지 못한 확인은 미확인으로
남깁니다. CLI 검사 통과를 에이전트의 지침 준수나 권한 확인으로 대신하지 않습니다.

임시 골격 정리는 plan의 확인 절차와 승인 범위에서만 합니다. 기존 소스나 운영
루트·작업 저장소 자신은 정리 대상이 아닙니다. 종료 승인도 별개입니다. 세팅을
아카이브하고 활성 규칙을 다시 읽은 뒤부터는 `.doltap/plans/workstreams.md` 등
적용된 프로젝트의 운영 문서가 원본입니다.

## 이전 구조의 문서 그래프 이관

기존 `.agents/`는 `.doltap/`로, `.doltap/project.md`는
`.doltap/plans/project.md`로 이관합니다. 운영 루트에서
`node <골격 위치>/bin/doltap.mjs migrate .`로 미리보고 승인 범위에서 `--apply`합니다.
두 위치가 함께 있거나 자동 이관할 수 없는 파일이 있으면 먼저 병합·보존 방법을
정합니다. 기존 ID와 역사적 산문은 보존하고 현재 링크·색인·경로 설명을 대조합니다.

이관은 문서 ID와 탐색 색인부터 만듭니다. 제목별 section이나 의미 관계를 자동으로
만들지 않습니다. 기존 문서를 부분 복사할 때 발급 기록을 실제 ID와 다른 상태로
덮어쓰지 않습니다. 제외하기로 한 작업 저장소에 ID를 발급하거나 운영 문서에서
ID 관계로 등록하지 않습니다.

중단된 쓰기는 `recover <실행-ID>`로 미리보고 `--apply`로 복구합니다.
`.doltap/recovery/`는 복구 자료이므로 커밋하지 않습니다. 성공·복구 결과를 확인한
실행만 `recover <실행-ID> --discard`로 정리합니다. 이 백업은 전체 편집 이력이
아닙니다. 자세한 명령은 [명령 참고](guide/reference.md), 훅은 [선택적 안내](guide/hooks.md)에 있습니다.
