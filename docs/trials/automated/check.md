# 기존 문서 구조 시험

[시험 보고서 목록](../README.md) · [시험 원본](../../../test/check.test.mjs)

## 방법

필수 파일·경로·current 색인·번호 충돌을 바꾼 임시 문서를 검사한다. 유효한 입력과 무시해야 할 예시·아카이브를 함께 비교한다.

- 실행: `node --test --test-reporter=tap test/check.test.mjs`
- 실행일: 2026-09-13 (Asia/Seoul)
- 환경: Node v22.14.0, win32 x64
- 시험 대상: 이 명령을 실행한 시점의 작업 트리
- 판정 기준: 종료 코드 0, TAP의 전체·통과·나열된 하위 시험 수가 모두 일치
- 시험 파일 SHA-256: `e7417d17c9d330b7d0f6e05d24427c1e02ee885eccbe2c31887204cfbdc61d20` (시험 파일만 식별하며 구현 전체의 해시는 아님)

## 결과

37개 실행, 37개 통과, 실패·건너뜀 0개. 다음은 실제 러너가 출력한 시험 이름이다.

| 번호 | 시험 | 결과 |
| --- | --- | --- |
| 1 | 갓 만든 골격에는 문제가 없다 | 통과 |
| 2 | CLAUDE.md가 AGENTS.md로 이어지지 않으면 잡는다 | 통과 |
| 3 | 안내표가 없는 파일을 가리키면 잡는다 | 통과 |
| 4 | 대작업마다 생기는 문서를 이름으로 부르는 것은 잡지 않는다 | 통과 |
| 5 | 코드 블록과 주석 안의 경로는 검사하지 않는다 | 통과 |
| 6 | 필수 문서가 빠지면 그 자리를 잡는다 | 통과 |
| 7 | 워크스트림에 status.md가 없고 current.md에도 안 적혔으면 둘 다 잡는다 | 통과 |
| 8 | plan.md·design.md·decisions.md는 없어도 문제가 아니고 있어도 문제가 아니다 | 통과 |
| 9 | 여러 줄짜리 안내는 메시지 열에 맞춰 들여쓴다 | 통과 |
| 10 | 워크스트림 번호가 겹치면 잡는다 | 통과 |
| 11 | current.md에 적힌 워크스트림 폴더가 없으면 잡는다 | 통과 |
| 12 | current.md의 주석 속 작성 예시는 적힌 것으로 세지 않는다 | 통과 |
| 13 | current.md의 날짜는 대작업 번호로 세지 않는다 | 통과 |
| 14 | current.md가 지나간 대작업을 말로 언급해도 활성으로 세지 않는다 | 통과 |
| 15 | current.md가 형식 없이 이름만 적어도 적힌 것으로 본다 | 통과 |
| 16 | 이름이 다른 이름의 일부로만 나오면 적힌 것으로 보지 않는다 | 통과 |
| 17 | 대작업이 동시에 여러 개여도 모두 적혀 있으면 통과한다 | 통과 |
| 18 | 동시에 여러 개일 때 하나만 빠져 있으면 그 하나만 잡는다 | 통과 |
| 19 | 채우기 자리를 다 채우면 통과에 들어간다 | 통과 |
| 20 | 절차를 설명하는 문장 속 낱말은 자리표시자로 세지 않는다 | 통과 |
| 21 | 활성 문서에서 표식 모양을 인용해도 자리표시자로 세지 않는다 | 통과 |
| 22 | 아카이브 안의 문서는 검사하지 않는다 | 통과 |
| 23 | 아카이브 폴더 이름이 형식에 어긋나도 잡지 않는다 | 통과 |
| 24 | 아카이브끼리 번호가 겹쳐도 잡지 않는다 | 통과 |
| 25 | 아카이브만 있고 진행 중인 대작업이 없어도 통과한다 | 통과 |
| 26 | history.md 가 없어진 아카이브를 가리키면 잡는다 | 통과 |
| 27 | 활성 문서가 지울 골격 안의 경로를 가리키면 알린다 | 통과 |
| 28 | 활성 문서가 골격 이름을 말로 설명하기만 하면 알리지 않는다 | 통과 |
| 29 | 적용을 마치고 지운 `.doltap-bootstrap` 을 세팅 기록이 가리켜도 문제가 아니다 | 통과 |
| 30 | AGENTS.md가 .doltap/로 안내하지 않으면 잡는다 | 통과 |
| 31 | 주석 속 @AGENTS.md는 연결로 보지 않는다 | 통과 |
| 32 | CLAUDE.md에 본문이 섞여 있으면 문제로 잡는다 | 통과 |
| 33 | 문제가 있으면 종료 코드 1로 끝낸다 | 통과 |
| 34 | 세팅 워크스트림이 활성이면 골격 참조 검사의 한계를 알린다 | 통과 |
| 35 | 세팅 워크스트림이 없으면 그 안내를 내지 않는다 | 통과 |
| 36 | 아카이브로 옮긴 세팅 워크스트림은 그 안내를 내지 않는다 | 통과 |
| 37 | current.md가 경로 형식만으로 적어도 적힌 것으로 본다 | 통과 |

## 해석 범위

이 파일은 기존 구조 검사 함수의 시험이다. 최종 CLI의 그래프 검사는 graph·workflow 시험과 함께 해석한다.
