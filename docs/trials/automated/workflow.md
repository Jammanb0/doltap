# 그래프 작업 흐름 시험

[시험 보고서 목록](../README.md) · [시험 원본](../../../test/workflow.test.mjs)

## 방법

임시 프로젝트에서 이관·편집·이동·삭제·검토·조회·훅 입출력을 실행한다. 오류 주입과 프로세스 강제 종료 후 복구도 검사한다.

- 실행: `node --test --test-reporter=tap test/workflow.test.mjs`
- 실행일: 2026-09-12 (Asia/Seoul)
- 환경: Node v22.14.0, win32 x64
- 시험 대상: 이 명령을 실행한 시점의 작업 트리
- 판정 기준: 종료 코드 0, TAP의 전체·통과·나열된 하위 시험 수가 모두 일치
- 시험 파일 SHA-256: `7de62ab594d364402f8656b6ed8c458dfef1102df4b0ab835dd0a88eabcf532c` (시험 파일만 식별하며 구현 전체의 해시는 아님)

## 결과

48개 실행, 48개 통과, 실패·건너뜀 0개. 다음은 실제 러너가 출력한 시험 이름이다.

| 번호 | 시험 | 결과 |
| --- | --- | --- |
| 1 | 새 프로젝트에는 복구 자료가 복사되지 않는다 | 통과 |
| 2 | 쓰기 미리보기와 적용의 JSON은 diff와 결과를 하나의 값으로 낸다 | 통과 |
| 3 | 발급 API로 기존 ID를 확인해도 레지스트리 문서 범위가 유지된다 | 통과 |
| 4 | 기존 절 안의 하위 절은 별도 ID를 받고 재실행 때 유지한다 | 통과 |
| 5 | 공백과 \#이 든 파일도 표준 URI 링크로 등록하고 복구한다 | 통과 |
| 6 | 일반 ID 링크도 잘못된 파일을 가리키면 오류다 | 통과 |
| 7 | 목록의 부모·자식 변경은 내용 해시를 바꾼다 | 통과 |
| 8 | 트랜잭션 실패는 새 파일과 기존 파일을 함께 되돌린다 | 통과 |
| 9 | 교체 직후 프로세스가 죽어도 예정 목록으로 복구한다 | 통과 |
| 10 | 사용자 편집과 손상 백업은 복구가 덮어쓰지 않는다 | 통과 |
| 11 | 프로젝트 밖 쓰기와 미리보기 이후 변경은 거부한다 | 통과 |
| 12 | 이관은 미리보기에서 쓰지 않으며 재실행해도 ID가 같다 | 통과 |
| 13 | 레거시 이관은 본문을 보존하고 색인으로 모든 문서를 잇는다 | 통과 |
| 14 | frontmatter는 ID 삽입 뒤에도 첫 블록이다 | 통과 |
| 15 | 정규화는 서식 차이를 없앤다: "hello world" | 통과 |
| 16 | 정규화는 서식 차이를 없앤다: "hello  world" | 통과 |
| 17 | 정규화는 서식 차이를 없앤다: "a\\r\\n\\r\\nb" | 통과 |
| 18 | 정규화는 의미·구조 변경을 남긴다: "- one\\n- two" | 통과 |
| 19 | 정규화는 의미·구조 변경을 남긴다: "\# title\\nbody" | 통과 |
| 20 | 정규화는 의미·구조 변경을 남긴다: "one  \\ntwo" | 통과 |
| 21 | 정규화는 의미·구조 변경을 남긴다: "```\\na  b\\n```" | 통과 |
| 22 | 정규화는 의미·구조 변경을 남긴다: "    a  b" | 통과 |
| 23 | 정규화는 의미·구조 변경을 남긴다: "word" | 통과 |
| 24 | 검토는 현재 내용에 묶이며 관계 추가는 이웃 검토를 만료시키지 않는다 | 통과 |
| 25 | 영향 없음은 구체적인 이유와 주체를 요구한다 | 통과 |
| 26 | 007: 활성 전제에서 아카이브의 검토 기록을 맥락으로 찾는다 | 통과 |
| 27 | 맥락은 작은 예산과 깊이를 지키고 생략을 표시한다 | 통과 |
| 28 | Git 없는 프로젝트와 gitignore된 운영 폴더의 지도는 같다 | 통과 |
| 29 | 감사는 외부 파일을 일회성 후보로 내고 관리 범위에 추가하지 않는다 | 통과 |
| 30 | 삭제는 문서 안의 자식으로 들어오는 관계도 거부한다 | 통과 |
| 31 | 아카이브 검사는 미해결·이월 연결·폐기 이유를 구분한다 | 통과 |
| 32 | 아카이브 검사는 문법 설명을 선언으로 세지 않고 자식 범위만 판정한다 | 통과 |
| 33 | project 설명 이관은 ID와 안팎의 상대 링크를 보존하고 반복 실행할 수 있다 | 통과 |
| 34 | project 설명이 두 위치에 있으면 자동 이관이 원본을 덮어쓰지 않는다 | 통과 |
| 35 | 구형 운영 폴더는 안내하고 복구 가능한 이관을 제공한다 | 통과 |
| 36 | 새 아카이브는 이관을 재실행해도 레거시로 바뀌지 않는다 | 통과 |
| 37 | 이동 복구는 라벨과 ID를 유지하고 경로와 발급 위치를 갱신한다 | 통과 |
| 38 | 중복 ID이면 이관과 경로 복구 모두 거부한다 | 통과 |
| 39 | 삭제 표식은 내용 대신 ID와 이유를 남기고 이후 발급에서도 보존된다 | 통과 |
| 40 | 대체 삭제는 참조를 옮기고 이전 ID로 supersedes를 보존한다 | 통과 |
| 41 | 완전 삭제 뒤 ID 재등장은 오류이며 재발급하지 않는다 | 통과 |
| 42 | 대칭 검토는 선언 방향이 바뀌어도 같은 영수증을 사용한다 | 통과 |
| 43 | 감사 판단 기록은 누적되며 --changed는 노드 검토를 사용한다 | 통과 |
| 44 | 문서 밖 본문과 주석 속 가짜 앵커를 구분한다 | 통과 |
| 45 | 선택적 Stop 어댑터는 JSON 판정과 재호출 종료를 제공한다 | 통과 |
| 46 | 이동한 외부 문서도 명시된 ID로만 찾아 복구한다 | 통과 |
| 47 | 세 노드 순환 의존을 지도에서 한 성분으로 표시한다 | 통과 |
| 48 | 기본 CLI는 그래프 위반에서 실패하고 임시 스키마 옵션을 받지 않는다 | 통과 |

## 해석 범위

훅은 stdin·출력 어댑터 시험이다. 실제 에이전트 설정에 설치한 실행을 의미하지 않는다.
