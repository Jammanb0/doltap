# 범위와 관계 파싱 시험

[시험 보고서 목록](../README.md) · [시험 원본](../../../test/graph.test.mjs)

## 방법

메모리 문서와 임시 파일에 정상·중복·교차 ID, 일반 링크와 관계 링크를 넣고 노드·엣지·오류를 대조한다.

- 실행: `node --test --test-reporter=tap test/graph.test.mjs`
- 실행일: 2026-09-14 (Asia/Seoul)
- 환경: Node v22.14.0, win32 x64
- 시험 대상: 이 명령을 실행한 시점의 작업 트리
- 판정 기준: 종료 코드 0, TAP의 전체·통과·나열된 하위 시험 수가 모두 일치
- 시험 파일 SHA-256: `4110047eccc23a14671ed5f7e81e9e3b65f7e92e37f2b02b192ee72cb605ae17` (시험 파일만 식별하며 구현 전체의 해시는 아님)

## 결과

49개 실행, 49개 통과, 실패·건너뜀 0개. 다음은 실제 러너가 출력한 시험 이름이다.

| 번호 | 시험 | 결과 |
| --- | --- | --- |
| 1 | 범위가 닫히면 노드가 된다 | 통과 |
| 2 | 닫히지 않은 범위를 잡는다 | 통과 |
| 3 | 교차하는 범위를 잡는다 | 통과 |
| 4 | 완전히 포함된 중첩은 잡지 않는다 | 통과 |
| 5 | name 과 id 의 값이 다르면 잡는다 | 통과 |
| 6 | ID 문자 집합을 벗어나면 잡는다 | 통과 |
| 7 | 같은 ID 가 두 곳에 있으면 잡는다 | 통과 |
| 8 | 문서 노드 밖의 범위를 잡는다 | 통과 |
| 9 | b 안에 s 를 두면 잡는다 | 통과 |
| 10 | 코드 울타리 안의 앵커는 선언이 아니다 | 통과 |
| 11 | 네 칸 들여쓴 코드 안의 앵커도 선언이 아니다 | 통과 |
| 12 | 목록 안쪽으로 들여쓴 줄은 코드가 아니다 | 통과 |
| 13 | 허용되지 않은 관계 유형을 잡는다 | 통과 |
| 14 | 없는 ID 를 가리키는 관계를 잡는다 | 통과 |
| 15 | 제목 조각을 관계 대상으로 쓰면 잡는다 | 통과 |
| 16 | 대칭 관계를 양쪽에 적으면 잡는다 | 통과 |
| 17 | 한쪽에만 적은 대칭 관계는 잡지 않는다 | 통과 |
| 18 | 관계를 한 번 적으면 양쪽에서 조회된다 | 통과 |
| 19 | 대칭 관계는 양쪽에서 같은 이름으로 읽힌다 | 통과 |
| 20 | 대칭 관계의 ID 는 선언 방향과 무관하게 같다 | 통과 |
| 21 | 없는 파일을 가리키는 일반 링크를 잡는다 | 통과 |
| 22 | 실재하는 일반 링크는 잡지 않는다 | 통과 |
| 23 | 진입점에서 닿지 않는 문서를 잡는다 | 통과 |
| 24 | 관계로 이어지면 도달로 본다 | 통과 |
| 25 | 사람용 판정과 JSON 판정이 같다 | 통과 |
| 26 | 깨끗한 그래프는 범위와 관계 수를 통과로 보고한다 | 통과 |
| 27 | 인라인 코드 안의 링크는 링크가 아니다 | 통과 |
| 28 | 인라인 코드 밖의 링크는 그대로 본다 | 통과 |
| 29 | 관계 선언 전체가 코드 예시면 세지 않는다 | 통과 |
| 30 | name 만 있는 앵커를 잡는다 | 통과 |
| 31 | id 만 있는 앵커도 잡는다 | 통과 |
| 32 | data-name 과 data-id 를 앵커 속성으로 오인하지 않는다 | 통과 |
| 33 | 같은 앵커 속성을 두 번 쓰면 잡는다 | 통과 |
| 34 | 외부 주소의 조각을 관계 대상으로 인정하지 않는다 | 통과 |
| 35 | b 안에 b 를 두면 잡는다 | 통과 |
| 36 | 같은 단방향 관계를 두 번 적으면 잡는다 | 통과 |
| 37 | 출발점이 다르면 같은 대상을 가리켜도 중복이 아니다 | 통과 |
| 38 | CLAUDE.md 는 앵커 없이 연결 노드가 된다 | 통과 |
| 39 | CLAUDE.md 가 없으면 새 스키마에서도 잡는다 | 통과 |
| 40 | CLAUDE.md 에 다른 본문이 있으면 연결 노드를 만들지 않는다 | 통과 |
| 41 | 관계로 가리킨 외부 문서를 관리 범위에 들인다 | 통과 |
| 42 | 가리키지 않은 외부 문서는 들이지 않는다 | 통과 |
| 43 | 관계가 저장소 밖을 가리켜도 외부 문서로 읽지 않는다 | 통과 |
| 44 | 관계로 가리켜도 숨김 폴더 문서는 관리 범위에 들이지 않는다 | 통과 |
| 45 | 관계로 가리켜도 복구와 의존성 폴더 문서는 관리 범위에 들이지 않는다 | 통과 |
| 46 | 관계로 가리켜도 마크다운이 아닌 파일은 관리 범위에 들이지 않는다 | 통과 |
| 47 | 운영 폴더 안의 숨김 폴더는 읽지 않는다 | 통과 |
| 48 | 저장소 밖을 가리키면 없다고 하지 않고 밖이라고 한다 | 통과 |
| 49 | 정말 없는 파일은 없다고 한다 | 통과 |

## 해석 범위

Markdown 전체 표준 구현이나 자연어 관계의 타당성을 검증하는 시험은 아니다.
